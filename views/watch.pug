doctype html
html(lang="ja")
    head
        meta(charset="utf-8")
        link(rel="stylesheet" href="style.css")
        script(src="mqttLindaForBrowser.js")
        script(src="//ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js")
        title mqtt_linda
    body
        h1 watching
        h2 watcing user
        div(id="main" data-init=initData data-ontuple=onTuple data-offtuple=offTuple)
            ul(id='content')

        script(src="watch.js")


