doctype html
html(lang="ja")
    head
        meta(charset="utf-8")
        link(rel="stylesheet" href="style.css")
        script(src="https://unpkg.com/mqtt/dist/mqtt.min.js")
        script(src="//ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js")
        title mqtt_linda
    body
        p Watching #{JSON.stringify(topic)}
        div(id="main" data-topic=JSON.stringify(topic))
            ul(id='content')
        script(src="lib.js")


